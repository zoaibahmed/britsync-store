import React from 'react';
import AboutWebflowClient from '@/components/AboutWebflowClient';

export const metadata = {
  title: 'About Us | Britsync — Global Heritage & Provenance Registry',
  description: 'Discover Britsync, a sovereign curation registry dedicated to preserving rare generational craftsmanship through cryptographic provenance passports, GI appellation enforcement, and direct artisan escrow.',
};

export default function AboutPage() {
  return <AboutWebflowClient />;
}
